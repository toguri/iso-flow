# AWS Configuration (for local development)
# AWS_PROFILE=your-profile-name
# AWS_REGION=your-region

# Local Development Database Configuration
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/iso_flow

# Test Database Configuration (for running tests locally)
# TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_iso_flow

# GitHub Actions OIDC Configuration (for CI/CD)
# This should be set as a GitHub Secret, not in .env
# AWS_ROLE_ARN=arn:aws:iam::YOUR_ACCOUNT_ID:role/YOUR_ROLE_NAME

# Note: Production database credentials should NEVER be stored in this file
# Production configurations should be managed through secure environment variables
# or secrets management systems (e.g., AWS Secrets Manager, Parameter Store)